# dependencies
node_modules/
# ...and as a bare symlink: agent worktrees link it in, and the trailing
# slash above does not match a symlink, so one got committed once.
node_modules

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
/ios
/android

# macOS
.DS_Store

# Metro
.metro-health-check*

# debug
npm-debug.*
*.log

# build artifacts
*.apk
*.aab

# icon design sources
assets/*-source.*

# env
.env
.env.*

# Throwaway git worktrees for parallel agent work — never committed.
.claude/worktrees/
